/**
 * Given the student name and class, return an object with the two information
 *
 * @param {String} studentName
 * @param {String} studentClass
 * @return {Object} an object with the keys "name" and "class", with values from the studentName and studentClass parameters respectively .
 *
 * @example
 * makeStudent("John", "1A01") // returns { name: "John", class: "1A01" }
 * makeStudent("Mary", "1A02") // returns { name: "Mary", class: "1A02" }
 * Note: Try accessing the name using both dot notation and bracket notation.
 */
function makeStudent(studentName, studentClass) {}

// Your own test cases
// e.g.;
console.log(makeStudent('John', '1A01')); // returns { name: "John", class: "1A01" }

module.exports = makeStudent;
