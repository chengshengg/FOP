/**
 * 10 - Swap the values of num1 and num2
 */
let a = 'a';
let b = 'b';

// You can use more variables if you need. (Hint: You need them)
const temp = a;
a = b;
b = temp;

console.log('Q10 ' + a + ' ' + b);

// Run the program with `node ./q10.js`
